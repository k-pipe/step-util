"""Basic tests for your package."""

import your_package_name


def test_version():
    """Test that the package version is defined."""
    assert hasattr(your_package_name, "__version__")
    assert isinstance(your_package_name.__version__, str)
